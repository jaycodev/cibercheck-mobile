<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_logo_cibercheck"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo_cibercheck"
        android:supportsRtl="true"
        android:theme="@style/Theme.CiberCheck"
        tools:targetApi="31">

        <!-- 👇 SplashScreen en MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginEmailActivity" android:exported="false" />
        <activity android:name=".VerificationCodeActivity" android:exported="false" />
        <activity android:name=".MisClasesActivity" android:exported="false" />
        <activity android:name=".HistorialAsistenciasActivity" android:exported="false" />
        <activity android:name=".MarcarAsistenciaActivity" android:exported="false" />
        <activity android:name=".MiPerfilActivity" android:exported="false" />




        <activity
            android:name=".ScanQRActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />
    </application>

</manifest>
